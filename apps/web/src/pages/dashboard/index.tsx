import { view } from '@rabjs/react';

export const DashboardPage = view(() => {
  return (
    <div className="flex-1 flex items-center justify-center">
      <p className="text-gray-500 dark:text-gray-400">仪表盘</p>
    </div>
  );
});

export default DashboardPage;
