# Ralph Progress Log
Started: 2026年 3月16日 星期一 15时33分24秒 CST
---

## Codebase Patterns
- Frontend service pattern: Extend Service from @rabjs/react, use observable properties, register in main.tsx with register()
- Frontend component pattern: Use view() wrapper and useService() hook for reactive dependency injection
- Settings routing: Nested routes under /settings with Outlet in SettingsPage, add menu items to settings-menu.tsx
- API client pattern: Use request helper from utils/request, wrap in try-catch, throw errors for controller handling
- Modal dialog pattern: Use fixed inset-0 with bg-black/50 backdrop, z-50 for top layer
- Backend service pattern: @Service() decorator for TypeDI, inject via constructor, use @echoe/logger for structured logging
- Backend controller pattern: @JsonController('/api/v1/resource'), @CurrentUser() for auth, @Body/@Param/@QueryParam for inputs
- Database queries: Always filter by uid AND isNull(deletedAt) for tenant isolation and soft-delete
- Drizzle schema pattern: varchar(191) for primary keys, timestamp with fsp:3, export from schema/index.ts with .js extension
- DTO pattern: Shared types in @echoe/dto package, build before consumers can import, use for type safety across frontend/backend
---

## 2026-03-16 16:00 - US-002
- Verified api_token schema implementation (already completed in previous iteration)
- Schema includes all required fields: token_id (PK), uid, name, token_hash, deleted_at, created_at, updated_at
- Indexes created for uid, token_hash, and deleted_at
- Schema properly exported from apps/server/src/db/schema/index.ts
- Typecheck passes successfully
- Files changed:
  - apps/server/src/db/schema/api-token.ts (already committed in f7a20e5)
  - apps/server/src/db/schema/index.ts (already committed in f7a20e5)
  - tasks/prd.json (updated passes status)
- **Learnings for future iterations:**
  - Drizzle schema pattern: Use mysqlTable() with varchar(191) for primary keys (MySQL utf8mb4 index limit)
  - Timestamp fields use `timestamp('field', { mode: 'date', fsp: 3 })` for millisecond precision
  - Indexes defined in second parameter of mysqlTable() using (table) => ({ indexName: index('name').on(table.field) })
  - All schemas must be exported from schema/index.ts with .js extension (e.g., './api-token.js')
  - Soft delete pattern uses deletedAt timestamp field with dedicated index
---
## 2026-03-16 18:35 - US-003
- Verified inbox schema implementation (already completed in previous iteration)
- Schema includes all required fields: inbox_id (PK), uid, front, back, source, category, is_read, deleted_at, created_at, updated_at
- Indexes created for: inbox_id (unique), uid, category, is_read, uid+is_read, uid+category, deleted_at
- Source defaults to 'manual', category defaults to 'backend'
- Schema properly exported from apps/server/src/db/schema/index.ts
- Typecheck passes successfully
- Files changed:
  - apps/server/src/db/schema/inbox.ts (already implemented)
  - apps/server/src/db/schema/index.ts (already exported)
  - tasks/prd.json (updated passes status)
- **Learnings for future iterations:**
  - Inbox schema uses camelCase field names (inboxId, isRead) but MySQL columns use snake_case (inbox_id, is_read)
  - Drizzle automatically maps between camelCase and snake_case
  - Composite indexes (uid+is_read, uid+category) improve query performance for common filter combinations
  - Text columns use `.notNull().$type<string>()` pattern for proper type inference
  - tinyint(1) is used for boolean flags (is_read: 0 = unread, 1 = read)
---
## 2026-03-16 18:37 - US-004
- Verified inbox_report schema implementation (already completed in previous iteration)
- Schema includes all required fields: inbox_report_id (PK), uid, date, content, summary, deleted_at, created_at, updated_at
- Unique constraint uid_date_unique ensures one report per user per day
- Indexes created for: inbox_report_id (unique), uid, date
- Date field uses varchar(10) for YYYY-MM-DD format storage
- Summary field stores JSON string for structured AI memory retrieval
- Schema properly exported from apps/server/src/db/schema/index.ts
- Typecheck passes successfully
- Files changed:
  - apps/server/src/db/schema/inbox-report.ts (already implemented)
  - apps/server/src/db/schema/index.ts (already exported)
  - tasks/prd.json (updated passes status)
- **Learnings for future iterations:**
  - Date field uses varchar(10) instead of DATE type for explicit YYYY-MM-DD string format
  - Summary field uses text().$type<string>() to store JSON as string (parsed at application layer)
  - Unique constraint on uid+date prevents duplicate daily reports at database level
  - This pattern supports idempotent report generation (409 conflict response)
---
## 2026-03-16 18:40 - US-005
- Verified migration generation (already completed in previous iteration)
- Server built successfully with pnpm --filter @echoe/server build
- Migration file 0001_known_dagger.sql includes all three tables:
  - api_token with uid, token_hash, deleted_at indexes
  - inbox with uid, category, is_read, uid+is_read, uid+category, deleted_at indexes
  - inbox_report with uid+date unique constraint and uid, date indexes
- Drizzle meta files updated: _journal.json includes migration entry 0001_known_dagger
- Snapshot files 0000_snapshot.json and 0001_snapshot.json present and consistent
- Typecheck passes successfully
- Files changed:
  - apps/server/drizzle/0001_known_dagger.sql (already generated)
  - apps/server/drizzle/meta/_journal.json (already updated)
  - apps/server/drizzle/meta/0001_snapshot.json (already updated)
  - tasks/prd.json (updated passes status)
- **Learnings for future iterations:**
  - Must run pnpm build before migrate:generate - drizzle-kit reads from dist/ not src/
  - Migration file naming uses format NNNN_random_name.sql (e.g., 0001_known_dagger.sql)
  - Drizzle automatically detects schema changes and only generates migration if needed
  - "No schema changes, nothing to migrate" means database schema matches code schema
  - Meta files (_journal.json, snapshot.json) track migration history and must be committed
---
## 2026-03-16 18:43 - US-006
- Verified DTO contracts implementation (already completed in previous iteration)
- Created DTO files:
  - api-token.dto.ts: ApiTokenDto, ApiTokenListItemDto, CreateApiTokenDto, CreateApiTokenResponseDto, DeleteApiTokenDto
  - inbox.dto.ts: InboxDto, InboxListItemDto, CreateInboxDto, UpdateInboxDto, InboxQueryParams, BulkUpdateInboxDto, BulkOperationResponseDto
  - inbox-report.dto.ts: InboxReportDto, InboxReportListItemDto, InboxReportQueryParams, InboxReportSummaryDto, UpsertInboxReportDto
  - inbox-to-card.dto.ts: AiOrganizeResponseDto (with optimizedFront, optimizedBack, reason, confidence, fallback), ConvertInboxToCardDto, AiOptimizeInboxDto, BulkConvertInboxToCardDto, ConversionResultDto, BulkConversionResponseDto, ConversionPreviewDto
- Defined enums: InboxSource, InboxCategory, CardConversionSource, CardConversionStatus
- All DTOs exported from packages/dto/src/index.ts
- Typecheck passes successfully
- Files changed:
  - packages/dto/src/api-token.dto.ts (already implemented)
  - packages/dto/src/inbox.dto.ts (already implemented)
  - packages/dto/src/inbox-report.dto.ts (already implemented)
  - packages/dto/src/inbox-to-card.dto.ts (already implemented)
  - packages/dto/src/index.ts (already exported)
  - tasks/prd.json (updated passes status)
- **Learnings for future iterations:**
  - DTO files use .dto.ts suffix for clear identification
  - Enums use UPPER_CASE values (e.g., MANUAL = 'manual') for consistency
  - AiOrganizeResponseDto includes fallback flag for degraded AI responses
  - Query params DTOs include pagination (page, limit), filtering, and sorting options
  - Bulk operation DTOs return success/failed counts with detailed error info
---
## 2026-03-16 18:45 - US-007
- Built DTO package successfully using `pnpm --filter @echoe/dto build`
- Verified server typecheck passes with new DTO exports
- Verified web typecheck passes with new DTO exports
- Build output includes index.js, index.mjs, index.d.ts with all new DTOs (api-token, inbox, inbox-report, inbox-to-card)
- Files changed:
  - packages/dto/dist/* (build output, gitignored)
  - tasks/prd.json (updated passes status)
- **Learnings for future iterations:**
  - DTO package uses Rollup to generate both CJS (index.js) and ESM (index.mjs) outputs
  - Build must complete successfully before consumers (server/web) can import new types
  - Typecheck validation ensures no missing-symbol errors in consuming packages
  - dist/ folder is gitignored - only source files are committed, build happens in CI/CD
  - The @echoe/dto package is symlinked in node_modules via pnpm workspace protocol
---
## 2026-03-16 18:46 - US-008
- Verified API token service implementation (already completed in previous iteration)
- Service includes all required methods:
  - createToken(): Generates plaintext token (40 hex chars), stores SHA256 hash, returns token once
  - listTokens(): Returns active tokens for user (filters by uid and deleted_at)
  - deleteToken(): Soft-deletes token by setting deleted_at timestamp
  - validateToken(): Validates plaintext token against stored hash for authentication
  - findTokenByIdAndUid(): Helper for ownership verification
- Uses generateApiTokenId() for business ID generation (prefix: 'at')
- All 10 tests pass successfully covering:
  - Token creation with unique plaintext generation
  - List operations with uid scoping
  - Soft-delete semantics
  - Token validation with SHA256 hashing
  - Error handling for not-found tokens
- Typecheck passes successfully
- Files changed:
  - apps/server/src/services/api-token.service.ts (already implemented in ebf65d2)
  - apps/server/src/__tests__/api-token.service.test.ts (already implemented)
  - tasks/prd.json (updated passes status)
- **Learnings for future iterations:**
  - API token service uses crypto.randomBytes(20).toString('hex') for 40-character hex tokens
  - SHA256 hashing via crypto.createHash('sha256').update(token).digest('hex')
  - Plaintext token returned only once at creation - never stored or retrievable again
  - Token validation queries by tokenHash index for performance
  - Soft-delete pattern: set deletedAt timestamp, filter with isNull(deletedAt) in queries
  - All database queries scoped by uid to prevent cross-tenant access
  - Service decorated with @Service() for TypeDI auto-registration
  - Uses @echoe/logger for structured logging (logger.info, logger.error)
---
## 2026-03-16 18:50 - US-009
- Verified API token authentication middleware implementation (already completed in previous iteration)
- Middleware parses Authorization: Bearer <token> header and validates token hash via ApiTokenService
- Sets authenticated user on request context (request.user and request.apiTokenId)
- Implements Token > JWT priority:
  - apiTokenAuthMiddleware runs BEFORE authHandler in app.ts middleware chain
  - authHandler checks if request.user is already set and skips JWT validation if present
  - When both Bearer token and JWT cookie are present, Bearer token takes precedence
- Invalid token handling:
  - Returns 401 when user is not found or soft-deleted
  - Returns 401 on any authentication errors
  - Gracefully skips to next middleware for missing/invalid Bearer format
- All 10 tests pass successfully covering:
  - No Authorization header (skip to next)
  - Invalid Bearer format (skip to next)
  - Invalid token (skip to next)
  - User not found (401)
  - Soft-deleted user (401)
  - Valid token authentication
  - Token > JWT priority verification
- Typecheck passes successfully
- Files changed:
  - apps/server/src/middlewares/api-token-auth.middleware.ts (already implemented)
  - apps/server/src/middlewares/auth-handler.ts (already has priority check)
  - apps/server/src/app.ts (middleware order already correct)
  - apps/server/src/__tests__/api-token-auth.middleware.test.ts (already implemented)
  - tasks/prd.json (updated passes status)
- **Learnings for future iterations:**
  - Middleware order matters: apiTokenAuthMiddleware must run BEFORE authHandler
  - Token > JWT priority implemented by checking if request.user is already set
  - Middleware should be lenient: skip to next() for missing/invalid Bearer format (let JWT handle it)
  - Only return 401 for valid Bearer token with authentication failures
  - Container.get() used to access services in middleware (TypeDI dependency injection)
  - Extends Express Request type with apiTokenId field for audit trail
  - Logger.debug for successful auth, logger.error for failures
  - Soft-deleted users (deletedAt > 0) should return 401 even if token is valid
---
## 2026-03-16 18:50 - US-010
- Verified API token controller implementation (already completed in previous iteration, commit ebf65d2)
- Controller implements all three required endpoints:
  - GET /api/v1/api-tokens: List all active tokens for authenticated user
  - POST /api/v1/api-tokens: Create new token, returns plaintext once
  - DELETE /api/v1/api-tokens/:tokenId: Soft-delete token by business ID
- Token management endpoints enforce JWT-only authentication:
  - Checks request.apiTokenId to detect API token auth
  - Returns 403 FORBIDDEN when API token auth is detected
  - Prevents token self-management lockout scenarios
- DELETE endpoint uses tokenId business ID (not numeric auto-increment id)
- Create response includes plaintext token with warning message
- All 16 controller tests pass successfully covering:
  - JWT authentication requirement for all endpoints
  - API token auth rejection for all endpoints
  - Token listing with empty results
  - Token creation with validation (name required, max 255 chars, trimming)
  - Token deletion with validation (tokenId required, not found handling, trimming)
  - Unauthorized handling for unauthenticated requests
- Typecheck passes successfully
- Files changed:
  - apps/server/src/controllers/v1/api-token.controller.ts (already committed in ebf65d2)
  - apps/server/src/__tests__/api-token.controller.test.ts (already committed in ebf65d2)
  - apps/server/src/controllers/index.ts (already updated in ebf65d2)
  - tasks/prd.json (updated passes status)
- **Learnings for future iterations:**
  - Controller pattern: @JsonController('/api/v1/resource') for base route
  - Method decorators: @Get(), @Post(), @Delete('/:param') for HTTP methods
  - Parameter decorators: @CurrentUser() for authenticated user, @Body() for request body, @Param() for path params, @Req() for raw request
  - JWT-only enforcement: Check request.apiTokenId to detect API token auth and reject with 403
  - Token self-management prevention: API tokens cannot manage themselves (prevents lockout)
  - Business ID usage: DELETE uses tokenId (business ID) not numeric id
  - Plaintext token security: Return only at creation time with explicit warning message
  - Error handling: Use ErrorCode constants and ResponseUtility.error/success wrappers
  - Input validation: Trim whitespace, check length, validate required fields
  - Service injection: Constructor injection via TypeDI (constructor(private service: Service) {})
---
## 2026-03-16 18:53 - US-011
- Verified inbox CRUD and read-state service implementation (already completed in previous iteration)
- Service implements all required methods:
  - create(): Creates inbox item with generateInboxId(), defaults source='manual' and category='backend'
  - list(): Returns paginated items with filters for category and isRead, ordered by createdAt DESC
  - update(): Updates inbox item with uid scoping and ownership verification
  - delete(): Soft-deletes by setting deletedAt timestamp
  - markRead(): Marks single inbox item as read (isRead=1)
  - markReadAll(): Marks all unread items as read, returns { updatedCount } count
  - findByIdAndUid(): Helper for ownership verification
- All operations scoped by uid and filter by isNull(deletedAt) for soft-delete semantics
- List supports pagination (page, pageSize) and filters (category, isRead)
- All 16 tests pass successfully covering:
  - Create with defaults, list with filters/pagination, update/delete with validation
  - markRead and markReadAll with count tracking
  - Error handling for not-found items
- Typecheck passes successfully
- Files changed:
  - apps/server/src/services/inbox.service.ts (already implemented)
  - apps/server/src/__tests__/inbox.service.test.ts (already implemented with 16 tests)
  - tasks/prd.json (updated passes status)
- **Learnings for future iterations:**
  - Service pattern: @Service() decorator for TypeDI auto-registration
  - All database queries must filter by uid AND isNull(deletedAt) for tenant isolation and soft-delete
  - Pagination pattern: page/pageSize with offset calculation, return total/totalPages
  - Filter building: Conditionally build where conditions array based on provided params
  - markReadAll pattern: Query unread items first to get count, then update in bulk
  - Ownership verification: Always check findByIdAndUid() before update/delete operations
  - Error handling: Throw descriptive errors ('Inbox item not found'), let controller handle HTTP status
  - Logger usage: logger.info for success operations, logger.error for failures
---
## 2026-03-16 18:57 - US-012
- Verified inbox controller implementation (already completed in previous iteration)
- Controller implements all required endpoints:
  - GET /api/v1/inbox: List inbox items with pagination (page, limit) and filters (category, isRead)
  - GET /api/v1/inbox/:inboxId: Get single inbox item by ID
  - POST /api/v1/inbox: Create new inbox item with validation (front and back required)
  - PUT /api/v1/inbox/:inboxId: Update inbox item with ownership verification
  - DELETE /api/v1/inbox/:inboxId: Soft-delete inbox item
  - POST /api/v1/inbox/:inboxId/read: Mark single inbox item as read
  - POST /api/v1/inbox/read-all: Mark all unread items as read, returns { updatedCount }
- All endpoints use DTOs from @echoe/dto for request/response typing
- Error handling returns consistent 4xx codes:
  - 401 UNAUTHORIZED for unauthenticated requests
  - 404 NOT_FOUND for missing inbox items
  - 400 PARAMS_ERROR for validation failures
- All 25 controller tests pass successfully covering:
  - List operations with filters and pagination
  - CRUD operations with validation
  - Read state management (single and bulk)
  - Error handling for not-found and unauthorized cases
- Typecheck passes successfully
- Files changed:
  - apps/server/src/controllers/v1/inbox.controller.ts (already implemented)
  - apps/server/src/__tests__/inbox.controller.test.ts (already implemented with 25 tests)
  - apps/server/src/controllers/index.ts (already exported)
  - tasks/prd.json (updated passes status)
- **Learnings for future iterations:**
  - Controller route pattern: @JsonController('/api/v1/resource') for base route, method decorators for HTTP verbs
  - Query parameter pattern: @QueryParam('name') for individual params, map to service call params
  - Pagination defaults: page=1, limit=20 when not provided
  - Validation pattern: Check required fields in controller, return PARAMS_ERROR before calling service
  - Error mapping: Catch service errors and map to appropriate HTTP status codes (NOT_FOUND, DB_ERROR)
  - Service injection: Constructor injection via TypeDI for clean dependency management
  - DTO usage: Import from @echoe/dto for type safety across frontend/backend boundary
  - GET /api/v1/inbox/:inboxId added for single item retrieval (bonus endpoint for frontend convenience)
---
## 2026-03-16 19:05 - US-013
- Verified AI organize context retrieval pipeline implementation (already implemented in previous iteration)
- InboxAiService includes all required features:
  - getL0Context(): Retrieves current inbox item for context
  - getL1Context(): Retrieves related inbox items from last 7 days (top 10) with similarity scoring
  - getL2Context(): Retrieves report summaries from last 30 days
  - buildContext(): Assembles complete context with all three levels
  - buildPrompt(): Builds AI prompt following PRD structure (task/currentInput/retrievedContext/constraints)
  - trimByPriority(): Trims context by similarity, recency, and diversity when exceeding token budget
- Fixed test mock setup issue:
  - Root cause: getDatabase() is called once and reused for multiple queries (stored in const db)
  - Previous mock setup created separate mock chains for each call, but service reuses the same db object
  - Solution: Made limitFn stateful to return different data for each query (queryCount-based logic)
  - All 10 tests now pass successfully
- Files changed:
  - apps/server/src/__tests__/inbox-ai.service.test.ts (fixed mock setup)
  - tasks/prd.json (updated passes status)
- **Learnings for future iterations:**
  - Service pattern: Single getDatabase() call reused for multiple queries (const db = getDatabase())
  - Test mock pattern: When db is reused, mock chain must be stateful (track queryCount in limitFn)
  - Context retrieval uses L0/L1/L2 hierarchy: current item, related items (7d), reports (30d)
  - Similarity scoring uses category match (0.4), length similarity (0.3), word overlap (0.3)
  - Token budget trimming: similarity first, then recency, then diversity
  - AI prompt structure: { task, currentInput, retrievedContext, constraints }
  - Test debugging tip: Add console.log to track mock call count and returned data
---
## 2026-03-16 19:10 - US-014
- Implemented AI organize execution with validation and fallback in InboxAiService.organizeInbox()
- Added AI model call using @ai-sdk/openai with temperature=0.2 for stable results
- Implemented 15-second timeout using AbortSignal.timeout() as per PRD requirements
- Added Zod schema validation for AI output (optimizedFront, optimizedBack, reason, confidence)
- Implemented comprehensive fallback logic:
  - Returns original front/back with fallback=true on timeout
  - Returns original front/back with fallback=true on JSON parse failure
  - Returns original front/back with fallback=true on schema validation failure
  - Returns original front/back with fallback=true when inbox item not found
- Added failure reason logging for observability using logger.error()
- Installed ai@^4.3.19 package (compatible with existing @ai-sdk/openai@0.0.52)
- Added 4 comprehensive tests covering all failure scenarios
- All 14 tests pass successfully
- Typecheck passes successfully
- Files changed:
  - apps/server/package.json (added ai dependency)
  - apps/server/src/services/inbox-ai.service.ts (added organizeInbox method)
  - apps/server/src/__tests__/inbox-ai.service.test.ts (added 4 new tests)
  - pnpm-lock.yaml (updated with ai package)
- **Learnings for future iterations:**
  - AI SDK version compatibility: @ai-sdk/openai@0.0.52 requires ai@^4.x, not ai@^6.x
  - Use createOpenAI() with baseURL config for custom OpenAI endpoints
  - AbortSignal.timeout() provides clean timeout handling for AI calls
  - Zod schema validation ensures type-safe AI output parsing
  - Fallback pattern: catch all errors, log detailed failure reason, return safe default
  - Test pattern: Use mockReturnValue() instead of mockReturnValueOnce() when getDatabase() is reused
  - AI organize uses buildPrompt() which includes L0/L1/L2 context for better results
  - Temperature 0.2 balances stability and creativity for organize tasks
---
## 2026-03-16 19:25 - US-015
- Implemented inbox report CRUD and idempotent generate endpoints
- Created InboxReportService with create, list, findByIdAndUid, and findByUidAndDate methods
- Created InboxReportController with three endpoints:
  - GET /api/v1/inbox/reports: List reports with pagination and date filters
  - GET /api/v1/inbox/reports/:reportId: Get single report by ID
  - POST /api/v1/inbox/reports/generate: Generate daily report with 409 conflict handling
- Added CONFLICT error code (7) to error-codes.ts for duplicate report handling
- Implemented 409 conflict response when report already exists for uid+date
- Added comprehensive tests: 10 service tests, 17 controller tests (all passing)
- Typecheck passes successfully
- Files changed:
  - apps/server/src/services/inbox-report.service.ts (new)
  - apps/server/src/controllers/v1/inbox-report.controller.ts (new)
  - apps/server/src/controllers/index.ts (registered controller)
  - apps/server/src/constants/error-codes.ts (added CONFLICT code)
  - apps/server/src/__tests__/inbox-report.service.test.ts (new, 10 tests)
  - apps/server/src/__tests__/inbox-report.controller.test.ts (new, 17 tests)
  - tasks/prd.json (updated passes status)
- **Learnings for future iterations:**
  - Report service uses findByUidAndDate to check for existing reports before creation
  - REPORT_ALREADY_EXISTS error message is thrown and caught in controller for 409 handling
  - 409 conflict response includes existing report reference (inboxReportId, date) for client convenience
  - Date validation uses regex /^\d{4}-\d{2}-\d{2}$/ for YYYY-MM-DD format
  - ResponseUtil.error only accepts 2 params (code, message), so conflict response must be built manually
  - List endpoint returns InboxReportListItemDto (excludes full content) for performance
  - Get endpoint returns full InboxReportDto including content field
  - Service list method supports date filters: exact date, startDate, endDate
  - Sort options: sortBy ('date' | 'createdAt'), order ('asc' | 'desc')
  - Placeholder report generation (TODO: implement AI report generation in US-016)
---
## 2026-03-16 19:35 - US-016
- Implemented AI report generation pipeline with 30-day memory and evidence
- Added generateDailyReport method to InboxAiService that:
  - Retrieves daily inbox items for the target date
  - Fetches 30-day report summaries for context (L2 memory)
  - Detects repeat suggestions from last 7 days and filters them out
  - Calls AI model with structured prompt including daily facts and memory context
  - Validates AI output with Zod schema (topics, mistakes, actions, insights with evidenceIds)
  - Returns fallback report with basic statistics when AI fails
- Updated InboxReportController to use AI generation instead of placeholder
- Updated controller tests to mock InboxAiService.generateDailyReport
- Added comprehensive tests for generateDailyReport (success and fallback cases)
- All 16 inbox-ai service tests pass, all 17 inbox-report controller tests pass
- Typecheck passes successfully
- Files changed:
  - apps/server/src/services/inbox-ai.service.ts (added generateDailyReport method)
  - apps/server/src/controllers/v1/inbox-report.controller.ts (integrated AI generation)
  - apps/server/src/__tests__/inbox-ai.service.test.ts (added 2 new tests)
  - apps/server/src/__tests__/inbox-report.controller.test.ts (updated mocks)
  - tasks/prd.json (updated passes status)
- **Learnings for future iterations:**
  - AI report generation uses 30s timeout (longer than organize's 15s) for more complex analysis
  - Report summary includes structured fields: topics, mistakes, actions, insights with evidenceIds
  - Repeat suggestion detection: parse last 7 days' report summaries and extract actions array
  - AI prompt instructs model to avoid repeating recent suggestions
  - Fallback report provides basic statistics when AI unavailable
  - Test mocking pattern: stateful orderBy/where functions to handle multiple query chains
  - Query pattern: first query has no .limit() (just .orderBy()), second query has .limit()
  - Fallback query in catch block has no .orderBy(), just .where()
  - Mock must handle all three query patterns correctly for tests to pass
---
## 2026-03-16 21:30 - US-017
- Implemented inbox-to-card manual conversion endpoint POST /api/v1/inbox/:inboxId/to-card
- Created InboxToCardController with deck/notetype ownership validation
- Validates required field mapping before note creation (default: front -> first field, back -> second field)
- Supports custom field mapping via fieldMapping parameter
- Creates note and card entities using EchoeNoteService.createNote()
- Returns noteId, cardId, deckId, notetypeId in response
- Added 11 comprehensive tests covering all scenarios
- All tests pass successfully
- Typecheck passes successfully
- Files changed:
  - apps/server/src/controllers/v1/inbox-to-card.controller.ts (new)
  - apps/server/src/controllers/index.ts (registered controller)
  - apps/server/src/__tests__/inbox-to-card.controller.test.ts (new, 11 tests)
  - tasks/prd.json (updated passes status)
- **Learnings for future iterations:**
  - Controller uses EchoeNoteService.createNote() to create notes with cards atomically
  - Field mapping pattern: default to first two fields (Front/Back), allow custom mapping
  - Notetype field definitions accessed via notetype.flds.map(f => f.name)
  - Single-field notetypes (cloze) only use front field, skip back field
  - Ownership validation order: inbox item -> deck -> notetype (fail fast)
  - EchoeNoteService.createNote() returns EchoeNoteWithCardsDto including cards array
  - Test mocks must include all DTO fields (EchoeDeckWithCountsDto has 15+ fields)
  - Error handling: specific errors for not-found vs validation failures
---
## 2026-03-16 21:32 - US-018
- Implemented AI-assisted inbox-to-card recommendation feature
- Updated InboxToCardController to support optional deckId/notetypeId parameters
- Added recommendDeckAndNotetype method using simple heuristics (category match, content length)
- Added getDefaultDeckAndNotetype fallback method for deterministic default mapping
- Added getAllNotetypes helper method to query active notetypes for user
- Made deckId and notetypeId optional in ConvertInboxToCardDto
- Response now includes aiRecommended flag, deckName, and notetypeName
- Updated tests to include InboxAiService mock and removed validation tests for optional fields
- Added test for aiRecommended flag in response
- All 10 inbox-to-card controller tests pass successfully
- All 94 inbox-related tests pass successfully
- Typecheck passes successfully
- Files changed:
  - apps/server/src/controllers/v1/inbox-to-card.controller.ts (added AI recommendation logic)
  - apps/server/src/__tests__/inbox-to-card.controller.test.ts (updated mocks and tests)
  - packages/dto/src/inbox-to-card.dto.ts (made deckId/notetypeId optional)
  - tasks/prd.json (updated passes status)
- **Learnings for future iterations:**
  - AI recommendation uses simple heuristics: category match to deck name, content length for notetype selection
  - Fallback pattern: Try AI recommendation first, catch errors and use deterministic default (first deck/notetype)
  - Response enrichment: Include human-readable names (deckName, notetypeName) alongside IDs for better UX
  - getAllNotetypes helper uses dynamic imports to avoid circular dependencies with db/connection
  - Test mocking pattern: Add new service dependencies to beforeEach setup with proper type casting
  - Optional fields in DTOs: Remove validation tests when fields become optional
  - aiRecommended flag distinguishes between explicit user choice vs AI/fallback selection
---
## 2026-03-16 21:48 - US-019
- Implemented InboxQueueService with centralized retry and timeout policies
- Created simple in-memory queue implementation (concurrency: 5) to avoid p-queue ESM import issues
- Added job definitions for generate_report, organize_inbox, and to_card_ai
- Configured retry policies matching PRD:
  - generate_report: 30s timeout, 2 retries with 30s delay
  - organize_inbox: 15s timeout, 2 retries with 10s delay
  - to_card_ai: 15s timeout, 2 retries with 10s delay (placeholder implementation)
- Updated InboxReportController to support async/sync execution paths:
  - Added async query param to /api/v1/inbox/reports/generate
  - Implemented idempotent report generation (checks existing report before generation)
  - Returns 409 conflict if report already exists
- Added organize endpoint to InboxController:
  - POST /api/v1/inbox/:inboxId/organize with optional ?async=true
  - Supports both sync (default) and async execution paths
- Added comprehensive tests (11 tests passing with proper timeout configuration for retry delays)
- All typecheck passes
- Files changed:
  - apps/server/src/services/inbox-queue.service.ts (new)
  - apps/server/src/__tests__/inbox-queue.service.test.ts (new)
  - apps/server/src/controllers/v1/inbox-report.controller.ts (updated)
  - apps/server/src/controllers/v1/inbox.controller.ts (updated)
  - apps/server/src/__tests__/inbox-report.controller.test.ts (updated)
  - apps/server/src/__tests__/inbox.controller.test.ts (updated)
  - apps/server/src/__tests__/inbox-to-card.controller.test.ts (updated)
  - tasks/prd.json (updated passes status)
- **Learnings for future iterations:**
  - p-queue v6 has ESM/CJS compatibility issues with Jest - implemented simple in-memory queue instead
  - Simple queue implementation: track pending count, process queue recursively with concurrency limit
  - pRetry library handles retry logic with exponential backoff and onFailedAttempt callbacks
  - Test timeout configuration: Use jest.setTimeout or test-level timeout for tests with retry delays
  - Controller pattern: Inject multiple services via constructor for clean dependency management
  - Async/sync execution pattern: Check query param, call enqueue() for async or executeSync() for sync
  - Idempotency pattern: Check for existing result before expensive operation, return existing if found
  - Queue service hooks: enqueue() for async, executeSync() for sync (both use same retry logic)
  - Test mocking pattern: Mock services before importing to avoid module initialization issues
  - Job configuration: Use Record<JobType, Config> for type-safe job config lookup
---
## 2026-03-16 21:55 - US-020
- Implemented InboxScheduledJobsService with two scheduled jobs
- Added weekly summary aggregation job (Sundays at 2 AM):
  - Aggregates inbox and report data for all users
  - Processes last 7 days of reports for AI memory context
  - Extracts and consolidates topics/actions from report summaries
- Added cleanup deleted records job (Daily at 3 AM):
  - Permanently deletes soft-deleted records older than 30 days
  - Cleans up inbox items, inbox reports, and API tokens
- Registered cron jobs in app.ts with proper startup/shutdown lifecycle
- Added 8 comprehensive tests covering all scenarios
- All tests pass successfully
- Typecheck passes successfully
- Files changed:
  - apps/server/src/services/inbox-scheduled-jobs.service.ts (new)
  - apps/server/src/__tests__/inbox-scheduled-jobs.service.test.ts (new)
  - apps/server/src/app.ts (added cron job registration and shutdown handlers)
  - tasks/prd.json (updated passes status)
- **Learnings for future iterations:**
  - Cron job pattern: Use cron.schedule() with timezone config from config.locale.timezone
  - Job wrapper pattern: Create async runJobName(trigger) function to handle errors and logging
  - Shutdown pattern: Store task references and call task.stop() in shutdown handler
  - Idempotent job pattern: Process each user independently, continue on individual failures
  - Cleanup pattern: Use lt() and isNotNull() to find soft-deleted records older than cutoff date
  - Weekly aggregation: Use selectDistinct() to get unique user IDs from multiple tables
  - Set deduplication: Use new Set<string>() to merge and deduplicate user IDs
  - JSON parsing safety: Wrap JSON.parse() in try-catch, continue on parse errors
  - Logging pattern: Log start/completion with metrics (processedUsers, deletedCount, duration)
  - Drizzle delete: result.rowsAffected ?? 0 handles missing rowsAffected field
---
## 2026-03-16 22:05 - US-021
- Implemented InboxMetricsService with centralized metrics emission and structured event logging
- Added comprehensive metrics tracking for all PRD 10.6 indicators:
  - Business metrics: inbox_create_total, inbox_organize_success_rate, inbox_organize_latency_p95, report_generate_success_rate, report_generate_latency_p95, to_card_success_rate, token_create_total, token_auth_success_rate
  - Technical metrics: ai_service_error_rate, ai_service_latency_p99, db_query_latency_p95, queue_job_backlog
- Integrated metrics tracking across all relevant services and controllers:
  - ApiTokenService: trackTokenCreate on token creation
  - ApiTokenAuthMiddleware: trackTokenAuthSuccess/trackTokenAuthFailure on authentication
  - InboxService: trackInboxCreate on inbox item creation
  - InboxAiService: trackInboxOrganizeStart/trackInboxOrganizeSuccess with timing and fallback flag
  - InboxReportController: trackReportGenerateStart/trackReportGenerateSuccess/trackReportGenerateConflict/trackReportGenerateError with timing
  - InboxToCardController: trackToCardStart/trackToCardSuccess/trackToCardError with AI recommendation flag
- Used @echoe/logger for all structured logs (no console logging)
- Tracked AI fallback count via fallback flag in trackInboxOrganizeSuccess
- Tracked report generation conflicts via trackReportGenerateConflict (409 responses)
- Updated all test files to mock InboxMetricsService dependency injection
- All 10 api-token service tests pass
- All 16 inbox service tests pass
- All 17 inbox-report controller tests pass
- Typecheck passes successfully
- Files changed:
  - apps/server/src/services/inbox-metrics.service.ts (new)
  - apps/server/src/services/api-token.service.ts (added metrics tracking)
  - apps/server/src/middlewares/api-token-auth.middleware.ts (added metrics tracking)
  - apps/server/src/services/inbox.service.ts (added metrics tracking)
  - apps/server/src/services/inbox-ai.service.ts (added metrics tracking with timing)
  - apps/server/src/controllers/v1/inbox-report.controller.ts (added metrics tracking with timing)
  - apps/server/src/controllers/v1/inbox-to-card.controller.ts (added metrics tracking)
  - apps/server/src/__tests__/*.test.ts (updated to mock InboxMetricsService)
  - tasks/prd.json (updated passes status)
- **Learnings for future iterations:**
  - Metrics service pattern: Create centralized service with enum-based metric types and event types
  - Structured logging pattern: Use logger.info with metadata objects, include timestamp in all events
  - Timing pattern: Record startTime = Date.now() at method start, calculate latency = Date.now() - startTime before tracking
  - Fallback tracking: Use boolean flag (fallback: true/false) to distinguish AI failures from successes
  - Conflict tracking: Track 409 responses as separate event type (trackReportGenerateConflict)
  - Test mocking pattern: Mock metrics service in beforeEach with all tracking methods as jest.fn()
  - Metrics emit pattern: logger.info with [METRIC] prefix and structured data (metric, value, metadata)
  - Event logging pattern: logger.info with [EVENT] prefix and structured data (event, metadata)
  - Error logging pattern: logger.error with [ERROR] prefix, error message, stack, and metadata
  - TypeDI dependency injection: Inject InboxMetricsService via constructor in all services/controllers
  - File extension: Always use .js extension for imports (inbox-metrics.service.js) in TypeScript with moduleResolution: node16
---
## 2026-03-16 22:07 - US-022
- Implemented web API clients for token, inbox, and report endpoints
- Created three new API files:
  - apps/web/src/api/api-token.ts: getApiTokens, createApiToken, deleteApiToken
  - apps/web/src/api/inbox.ts: CRUD operations, markRead/markAllRead, organizeInboxItem, convertInboxToCard
  - apps/web/src/api/inbox-report.ts: getInboxReports, getInboxReport, generateInboxReport
- All API functions use shared DTO contracts from @echoe/dto for type safety
- Implemented explicit error handling:
  - 409 conflict for duplicate report generation (returns existing report reference)
  - 503 AI unavailable responses with user-friendly error messages
- Typecheck passes successfully
- Files changed:
  - apps/web/src/api/api-token.ts (new)
  - apps/web/src/api/inbox.ts (new)
  - apps/web/src/api/inbox-report.ts (new)
  - tasks/prd.json (updated passes status)
- **Learnings for future iterations:**
  - API client pattern: Use request helper from ../utils/request for consistent error handling
  - Response typing: Use ApiResponseDto<T> wrapper for all API responses
  - Error handling pattern: Wrap API calls in try-catch to handle specific HTTP status codes (409, 503)
  - Pagination response: Define custom interfaces (InboxListResponse, InboxReportListResponse) for paginated data
  - Async execution: Support optional ?async=true query param for queue-based execution
  - 409 conflict handling: Extract existing resource from error response and throw structured error
  - 503 handling: Throw user-friendly error message for AI service unavailability
  - DTO imports: Import all required types from @echoe/dto for type safety across frontend/backend
  - Query params: Pass as { params } in request.get() second argument
  - API endpoints follow REST conventions: GET/POST/PUT/DELETE with resource IDs in path
---
## 2026-03-16 22:15 - US-023
- Implemented settings API token management UI at /settings/api-tokens
- Created ApiTokenService for state management:
  - loadTokens(): Fetches all API tokens for current user
  - createToken(name): Creates new token, returns plaintext once
  - deleteToken(tokenId): Soft-deletes token by business ID
  - Observable state: tokens, isLoading, error
- Created ApiTokenSettings component with full CRUD UI:
  - Token list view with name, creation date, and token ID
  - Create dialog with name input and validation (max 255 chars)
  - Delete confirmation flow (two-step: confirm button → actual delete)
  - Created token dialog shows plaintext token only once with copy button
  - Warning message: "请立即复制并保存此 Token，关闭后将无法再次查看"
- Added API Token menu item to settings sidebar with Key icon (lucide-react)
- Registered ApiTokenService globally in main.tsx with register()
- Added /settings/api-tokens route in App.tsx as nested route under /settings
- All typecheck passes successfully
- Commit: f9c3177
- Files changed:
  - apps/web/src/services/api-token.service.ts (new)
  - apps/web/src/pages/settings/components/api-token-settings.tsx (new)
  - apps/web/src/pages/settings/components/settings-menu.tsx (added Key icon and menu item)
  - apps/web/src/main.tsx (registered ApiTokenService)
  - apps/web/src/App.tsx (added route)
  - tasks/prd.json (updated passes status)
- **Learnings for future iterations:**
  - Service pattern: Extend Service class from @rabjs/react, use observable properties for reactive state
  - Component pattern: Use view() wrapper and useService() hook for dependency injection
  - Dialog pattern: Use fixed inset-0 with bg-black/50 backdrop for modal dialogs
  - Token security: Show plaintext token only once in modal dialog, cannot retrieve again after closing
  - Copy to clipboard: Use navigator.clipboard.writeText() with toast feedback
  - Delete confirmation: Two-step flow (confirm button → actual delete) to prevent accidental deletion
  - Settings routing: Nested routes under /settings with Outlet in SettingsPage
  - Service registration: Must register service in main.tsx before using in components
  - Menu icons: Import from lucide-react, use consistent w-5 h-5 sizing
  - TailwindCSS dark mode: Use dark: prefix for dark theme styles (dark:bg-dark-800, dark:text-gray-50)
  - Form validation: Trim whitespace, check length, show toast warnings for validation errors
  - Loading states: Use isLoading flag with spinner animation and disabled buttons
  - Empty states: Show icon + message when no data (Key icon with "暂无 API Token")
---
