# Ralph Progress Log
Started: 2026年 3月17日 星期二 CST
---

## Codebase Patterns
- Database schemas: Use int auto-increment for id, varchar(191) for uid/name (MySQL utf8mb4 index limit)
- Tenant-scoped entities: Always add unique constraint on (uid, name) for multi-tenant name fields
- Timestamps: Use `timestamp('created_at', { mode: 'date', fsp: 3 })` for millisecond precision
- Schema exports: Must export from schema/index.ts using .js extension (drizzle-kit reads compiled JS)
- Migrations: Always build server first (`pnpm --filter @echoe/server build`) before `migrate:generate`
- DTOs: Create separate DTO file for related entities, export from index.ts with .js extension
- DTO list responses: Include items array and total count
- Services: Use @Service() decorator from typedi, ensure uid scoping in all queries
- Auto-seeding: Check count on first list() call, seed defaults if count = 0
- Idempotent create: Check if entity exists before inserting, return existing if found
- Delete pattern: Fetch entity first, update related records to null, then delete entity
- Controllers: Use @JsonController('/api/v1/resource'), inject services via constructor, must export from controllers/index.ts
- Controller auth: Use @CurrentUser() decorator, always check if userDto?.uid exists before processing
- Controller validation: Trim whitespace from user input, validate required fields, return proper error codes
- Controller delete: Use @Delete('/:id') with @Param('id'), parse and validate ID, return success message
- Controller responses: Use ResponseUtil.success() and ResponseUtil.error() for consistency
- Controller tests: Mock services, test happy path + error cases, use ! operator for result.data after null check

## 2026-03-17 09:50 - US-001
- Implemented inbox_source and inbox_category database schemas
- Files changed:
  - apps/server/src/db/schema/inbox-source.ts (new)
  - apps/server/src/db/schema/inbox-category.ts (new)
  - apps/server/src/db/schema/index.ts (added exports)
- **Learnings for future iterations:**
  - Schema pattern: Use int auto-increment for id, varchar(191) for uid/name fields (MySQL utf8mb4 index limit)
  - Always add unique constraint on (uid, name) for tenant-scoped name fields
  - Timestamp pattern: `timestamp('created_at', { mode: 'date', fsp: 3 })` for millisecond precision
  - Must export from schema/index.ts using .js extension (drizzle-kit reads compiled JS)
  - Typecheck must pass before committing
---

## 2026-03-17 09:55 - US-002
- Generated database migration for inbox_source and inbox_category tables
- Files changed:
  - apps/server/drizzle/0007_sad_micromacro.sql (new)
- **Learnings for future iterations:**
  - Always build server first: `pnpm --filter @echoe/server build` before `migrate:generate`
  - Drizzle-kit reads from dist/ folder (compiled JS), not source TypeScript
  - Migration SQL should be reviewed to verify proper table structure and indexes
  - Unique constraints and indexes are correctly generated from schema definitions
---

## 2026-03-17 10:00 - US-003
- Created DTOs for inbox source and category
- Files changed:
  - packages/dto/src/inbox-source-category.dto.ts (new)
  - packages/dto/src/index.ts (added export)
- **Learnings for future iterations:**
  - DTO pattern: Create separate DTO file for related entities
  - Always export from index.ts with .js extension (for compiled output)
  - Build dto package after changes: `pnpm --filter @echoe/dto build`
  - DTO interfaces should include all fields from database schema plus response wrappers
  - List response DTOs typically include items array and total count
---

## 2026-03-17 10:05 - US-004
- Implemented InboxSourceService and InboxCategoryService with full CRUD operations
- Files changed:
  - apps/server/src/services/inbox-source.service.ts (new)
  - apps/server/src/services/inbox-category.service.ts (new)
  - apps/server/src/__tests__/inbox-source.service.test.ts (new)
  - apps/server/src/__tests__/inbox-category.service.test.ts (new)
- **Learnings for future iterations:**
  - Service pattern: Use @Service() decorator from typedi for dependency injection
  - Auto-seeding pattern: Check count on first list() call, seed defaults if count = 0
  - Idempotent create: Check if entity exists by (uid, name) before inserting
  - Always ensure uid scoping in all database queries for multi-tenant safety
  - Use sql<number>\`count(*)\` for count queries in Drizzle
  - Service tests: Mock getDatabase() and chain methods (select, from, where, limit, orderBy)
  - Test coverage: Test happy path, edge cases (not found, already exists), and seeding logic
  - Default sources: manual, web, api, extension, other
  - Default categories: backend, frontend, design, product, life, other
---
## 2026-03-17 10:05 - US-005
- Implemented InboxSourceController and InboxCategoryController with list and create endpoints
- Files changed:
  - apps/server/src/controllers/v1/inbox-source.controller.ts (new)
  - apps/server/src/controllers/v1/inbox-category.controller.ts (new)
  - apps/server/src/controllers/index.ts (added exports)
  - apps/server/src/__tests__/inbox-source.controller.test.ts (new)
  - apps/server/src/__tests__/inbox-category.controller.test.ts (new)
- **Learnings for future iterations:**
  - Controller pattern: Use @JsonController('/api/v1/resource') for base route
  - Use @Get() and @Post() decorators for HTTP methods
  - Constructor injection for services: `constructor(private myService: MyService) {}`
  - Use @CurrentUser() decorator to get authenticated user (returns UserInfoDto | undefined)
  - Always check if user is authenticated before processing request
  - Validate required fields (e.g., name not empty) before calling service
  - Trim whitespace from user input before processing
  - Return proper error codes: UNAUTHORIZED, PARAMS_ERROR, DB_ERROR
  - Use ResponseUtil.success() and ResponseUtil.error() for consistent responses
  - Controllers must be exported from controllers/index.ts to be auto-loaded by IOC container
  - Test pattern: Mock services, test happy path + error cases (unauthorized, validation, db errors)
  - Use non-null assertion operator (!) in tests when accessing result.data after checking it's not null
  - List endpoints return wrapper objects with items array and total count
---

## 2026-03-17 10:10 - US-006
- Implemented DELETE endpoints for inbox sources and categories with proper cleanup
- Files changed:
  - apps/server/src/controllers/v1/inbox-source.controller.ts (added DELETE endpoint)
  - apps/server/src/controllers/v1/inbox-category.controller.ts (added DELETE endpoint)
  - apps/server/src/services/inbox-source.service.ts (updated delete method to clear inbox records)
  - apps/server/src/services/inbox-category.service.ts (updated delete method to clear inbox records)
  - apps/server/src/__tests__/inbox-source.controller.test.ts (added delete tests)
  - apps/server/src/__tests__/inbox-category.controller.test.ts (added delete tests)
  - apps/server/src/__tests__/inbox-source.service.test.ts (updated delete tests)
  - apps/server/src/__tests__/inbox-category.service.test.ts (updated delete tests)
- **Learnings for future iterations:**
  - Delete pattern: First fetch the entity to get its name, then update related records, then delete
  - When deleting a source/category, set inbox.source or inbox.category to null (don't delete inbox records)
  - Import inbox schema in service files to enable updating inbox records
  - Use @Delete() decorator with @Param('id') for DELETE endpoints
  - Parse string ID to number with parseInt() and validate with isNaN()
  - Return success message in response: { message: 'Source deleted successfully' }
  - Test delete endpoint with: success case, unauthorized, invalid ID, and database error
  - Test service delete with: success case and not found case (should return early without error)
  - Mock user uid in tests must match the uid used in test assertions
---

## 2026-03-17 10:15 - US-007
- Updated inbox schema to support dynamic source and category values
- Files changed:
  - apps/server/src/db/schema/inbox.ts (changed source and category to nullable varchar(191))
  - apps/server/src/services/inbox-ai.service.ts (updated calculateSimpleSimilarity to handle nullable categories)
  - apps/server/drizzle/0008_majestic_mimic.sql (new migration)
- **Learnings for future iterations:**
  - When changing field types from NOT NULL to nullable, dependent code must be updated
  - Nullable fields in Drizzle: omit .notNull() and .default() to make field nullable
  - varchar(191) is the standard length for indexed string fields (MySQL utf8mb4 limit)
  - Migration correctly generates ALTER TABLE MODIFY COLUMN for type changes
  - When updating method signatures to accept nullable types, use `| null | undefined` for flexibility
  - Check for null/undefined before using nullable fields (use `if (value && ...)` pattern)
---

## 2026-03-17 10:20 - US-008
- Updated inbox DTOs to support dynamic source and category values
- Files changed:
  - packages/dto/src/inbox.dto.ts (changed source/category from enum to string | null in InboxDto, InboxListItemDto, CreateInboxDto, UpdateInboxDto, InboxQueryParams, BulkUpdateInboxDto)
  - apps/server/src/services/inbox.service.ts (updated UpdateInboxParams to accept string | null for source/category)
  - apps/web/src/services/inbox.service.ts (removed InboxCategory import, updated filters to use string)
  - apps/web/src/pages/inbox/inbox-page.tsx (removed InboxCategory import, updated all type references to use string)
- **Learnings for future iterations:**
  - When changing DTO field types, must update both backend service interfaces AND frontend components
  - Frontend state management (Service classes) must be updated to match DTO changes
  - Remove unused enum imports after converting to string types
  - Nullable fields in DTOs should be `string | null`, while optional fields can be `string | undefined`
  - Pre-existing type errors in the web app are not related to this change - focus on errors specific to the task
  - When a migration file gets auto-reordered by drizzle-kit, restore it if it's already applied to avoid confusion
---

## 2026-03-17 10:25 - US-009
- Updated InboxService to auto-create source and category on inbox creation
- Files changed:
  - apps/server/src/services/inbox.service.ts (added InboxSourceService and InboxCategoryService injection, added auto-creation logic)
  - apps/server/src/__tests__/inbox.service.test.ts (added mocks for new services, added 3 new test cases)
- **Learnings for future iterations:**
  - Service dependency injection: Inject related services via constructor to enable cross-service operations
  - Auto-create pattern: Call service.create() before using the value - the service handles idempotency (returns existing if found)
  - Backward compatibility: Maintain default values ('manual', 'backend') when source/category not provided
  - Test pattern: Mock injected services in beforeEach, verify service method calls with toHaveBeenCalledWith()
  - The InboxSourceService.create() and InboxCategoryService.create() methods are idempotent - they check if entity exists first
---
## 2026-03-17 10:30 - US-010
- Updated InboxService.update() method to support dynamic source/category on update
- Files changed:
  - apps/server/src/services/inbox.service.ts (added auto-creation logic for source/category in update method)
  - apps/server/src/__tests__/inbox.service.test.ts (added 4 new test cases for update functionality)
- **Learnings for future iterations:**
  - Update pattern: Auto-create source/category before updating, similar to create method
  - Null handling: Check for both undefined and null - auto-create only when value is provided AND not null
  - Setting to null: Support explicitly setting source/category to null (for clearing values)
  - Test coverage for update: Test auto-create source, auto-create category, set source to null, set category to null
  - The auto-create logic should be placed BEFORE building update values to ensure entities exist
  - Mock service.create should not be called when setting values to null
---
